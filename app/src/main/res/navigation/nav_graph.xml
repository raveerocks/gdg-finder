<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="io.raveerocks.gdgfinder.ui.HomeFragment"
        android:label="Home"
        tools:layout="@layout/home_fragment">
        <action
            android:id="@+id/action_homeFragment_to_gdgListFragment"
            app:destination="@id/gdg_search" />
        <action
            android:id="@+id/action_homeFragment_to_addGdgFragment"
            app:destination="@id/gdg_apply" />
    </fragment>

    <fragment
        android:id="@+id/gdg_search"
        android:name="io.raveerocks.gdgfinder.ui.GdgListFragment"
        android:label="GDG Search"
        tools:layout="@layout/list_item"/>

    <fragment
        android:id="@+id/gdg_apply"
        android:name="io.raveerocks.gdgfinder.ui.AddGdgFragment"
        android:label="Apply"
        tools:layout="@layout/add_gdg_fragment" />

</navigation>